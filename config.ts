// Config to control feature flags
// Set to false to hide ALL premium/payment features (for Apple App Store Review compliance)
export const SHOW_PREMIUM_FEATURES = true;
// Set to true to give new users automatic premium (beta promo)
export const IS_BETA_PROMO_ACTIVE = true;
